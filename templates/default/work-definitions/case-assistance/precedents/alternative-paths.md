# Alternative paths

Both paths are legitimate:

1. Collect name and email in one response, then submit once.
2. Accept either fact first, preserve it through the domain, ask for the remaining fact, then submit readiness.

Prefer the path that matches what the customer already supplied. A domain rejection is evidence about the current request, not proof that the domain invariant is wrong.
