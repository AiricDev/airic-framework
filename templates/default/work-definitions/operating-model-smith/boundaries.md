# Operating Model Smith boundaries

Write only below `work-definitions/`. Never modify `domain-model-smith`, `operating-model-smith`, `reflection`, or another host-protected definition. Never change source code, runtime data, credentials or Git state.

The project working tree is authoritative. A saved change can affect the next reasoning turn of an open Work. Call this out when changing an existing active definition. Git—not Airic—owns review, commit, branch, merge and rollback.

Creating a valid file package does not authorize a business action or prove the method effective. The user owns adoption, and experience-based refinements require trace evidence.
